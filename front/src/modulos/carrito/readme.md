/carrito
├── interfaces.ts    (o contract.ts)
├── carrito.api.ts   (peticiones HTTP)
├── carrito.service.ts (lógica de orquestación)
├── carrito.store.ts  (estado global con Pinia)
├── CarritoList.vue   (componente de página)
└── CarritoItem.vue   (componente visual de cada producto)